# Cloudflare Worker Integration

Block malicious IPs at the edge before they reach your server.

## Usage
1. Create a new Worker in Cloudflare Dashboard.
2. Paste the content of `worker.js`.
3. Deploy to your route.
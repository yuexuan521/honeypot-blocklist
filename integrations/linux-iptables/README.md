# Linux Iptables & IPSet Integration

High-performance blocking using kernel-level IP sets.

## Requirements
* `ipset`
* `iptables`

## Usage
Run the script to fetch the latest IPs and ban them instantly:
```bash
sudo bash block_ips.sh
```
